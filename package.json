{
  "name": "flint",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --filter=@flint/web",
    "dev:mobile": "turbo run dev --filter=@flint/mobile",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "workspaces": ["apps/*", "packages/*"],
  "packageManager": "bun@1.2.0"
}
